<IfModule mod_deflate.c>
    # Nivel de compresión: rango 1 (más rápido, menos compresión) a 9 (más lento, máxima compresión)
    DeflateCompressionLevel 9

    # Aplica el filtro DEFLATE solo a estos tipos MIME (contenido de texto)
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE text/javascript
    AddOutputFilterByType DEFLATE application/json
    AddOutputFilterByType DEFLATE image/svg+xml

    # Evita compresiones innecesarias
    SetEnvIfNoCase Request_URI \.(?:gif|jpe?g|png|webp|mp4|avi|zip|gz|pdf)$ no-gzip dont-vary

    # Evita comprimir si el cliente es un navegador antiguo con soporte incompleto de DEFLATE
    BrowserMatch ^Mozilla/4 gzip-only-text/html
    BrowserMatch ^Mozilla/4\.0[678] no-gzip
    BrowserMatch \bMSIE !no-gzip !gzip-only-text/html
</IfModule>